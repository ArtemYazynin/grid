/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CellPipe } from './cell.pipe';

describe('Pipe: Celle', () => {
  it('create an instance', () => {
    let pipe = new CellPipe();
    expect(pipe).toBeTruthy();
  });
});
